#pragma once

#include <FrenchieApplicationLayer.hpp>

// STL
#include <string>
#include <vector>
#include <map>
#include <set>

namespace Frenchie
{
    namespace Editor
    {
        namespace Helpers
        {
            void draw_menu(const std::string& _MenuPath, void* _Sender = nullptr);
        }
    }
}